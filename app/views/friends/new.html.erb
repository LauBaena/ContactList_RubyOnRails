<h1>Add New Friend</h1>

<!-- It's pulling a partial (with render). This partial is the one called _form.html.erb -->
<%= render "form", friend: @friend %>

<br>

<div>
  <%= link_to "Back to friends", friends_path %>
</div>
